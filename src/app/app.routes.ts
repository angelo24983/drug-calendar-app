import { Routes } from '@angular/router';
import { PrescrizioneListComponent } from './components/prescrizione-list/prescrizione-list.component';

export const routes: Routes = [
    {
        path: '',
        component: PrescrizioneListComponent,
    }
];
